"""Independent v2 supervisor nodes (Phase 2, Task 1).

Package marker only (ruling R2): every consumer imports node functions from the
sibling submodules so later tasks can add modules without merge conflicts.
"""
